#pragma once

// WiFi credentials — copy this file to secrets.h and fill in real values.
// secrets.h is gitignored; never commit real credentials.
#define SECRET_SSID "your-wifi-ssid"
#define SECRET_PASS "your-wifi-password"
